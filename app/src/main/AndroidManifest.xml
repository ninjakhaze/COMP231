<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fitness.buddy.comp231">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/bgblack"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_baseline_emoji_emotions_24"
        android:supportsRtl="true"
        android:theme="@style/Theme.COMP231">
        <activity android:name=".ChangePasswordActivity" />
        <activity android:name=".ForgetPasswordActivity" />
        <activity android:name=".StartActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ChatRoom"/>
        <activity android:name=".TrainerList"/>
        <activity android:name=".ProfileActivity"/>
        <activity android:name=".PremiumActivty"/>
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".MessageActivity"
            android:windowSoftInputMode="adjustPan"/>
    </application>

</manifest>